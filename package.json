{
  "name": "the-anthology",
  "version": "1.0.0",
  "description": "A personal poetry and quotes collection",
  "main": "server/index.js",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "node --no-warnings=ExperimentalWarning server/index.js",
    "seed": "node --no-warnings=ExperimentalWarning server/seed.js",
    "setup": "npm install --prefix client && npm run build --prefix client",
    "dev:server": "node --no-warnings=ExperimentalWarning server/index.js",
    "dev:client": "npm run dev --prefix client"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.3"
  }
}
